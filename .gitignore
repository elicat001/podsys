# python
backend/.venv/
__pycache__/
*.pyc
.env

# data / generated outputs
backend/data/

# 本地 AI 模型(超分 Swin2SR onnx / FSRCNN pb 等,体积大,不入库;缺失时代码自动降级 Lanczos)
# 忽略目录内容,但保留放置说明 README(告诉别人该下载哪些文件、放哪)
backend/models/*
!backend/models/README.md
*.onnx
*.pb

# 本地 IDE/启动配置 + Claude Code 本地权限白名单(含机器相关绝对路径,每人各自维护,不入库)
.claude/launch.json
.claude/settings.local.json

# 本地开发工具链(只在本机用,不入库)
backend/pyproject.toml
backend/requirements-dev.txt
backend/e2e/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# os
Thumbs.db
.DS_Store

# 根目录运行时数据(预览/直接在根目录跑时生成)
/data/
data/
*.db
