fastapi>=0.115
uvicorn[standard]>=0.30
pillow>=10.4
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
pyjwt>=2.8
openai>=1.55
pytest>=8.0
# 异步作业队列:Celery + 独立 Redis 实例(默认 127.0.0.1:6380,与旁边 Django 项目的
# 6379/db1 物理隔离)。结果存 Job 表,不用 Celery result backend。测试走 eager 不需 broker。
# 部署需:① 另起一个 redis-server 实例跑 6380;② 一个 podsys-worker.service 跑 celery worker。
celery>=5.3
redis>=5.0
# 本地抠图引擎(默认 POD_MATTING_PROVIDER=rembg 时必需;首次运行会下载 ~170MB U2Net 模型)
rembg>=2.0.59
onnxruntime>=1.18    # rembg 的推理后端(U2Net)+ 图像提质 Real-ESRGAN(models/realesr_x4v3.onnx,~4.7MB,不入库,缺失降级 Lanczos)
# 印花提取的本地去布料(洪水填充)用到;通常随 rembg 一起装上
numpy>=1.26
scipy>=1.11
# cv2:矢量化(轮廓追踪)+ 图像提质(去噪/detailEnhance)。用 contrib-headless(全功能超集);
# 别同时装 opencv-python(会冲突坏 cv2)。
opencv-contrib-python-headless>=4.8
# 转矢量图主引擎:vtracer 工业级彩色描摹(平滑贝塞尔曲线 + 去碎块);缺失时降级到 cv2 轮廓追踪
vtracer>=0.6
# 本地标题 OCR:从设计图识别文字当标题主体。pytesseract 只是薄封装(不拉 opencv,无依赖冲突);
# **还需系统装 tesseract 二进制**:生产 `apt install -y tesseract-ocr tesseract-ocr-eng`,
# Windows `winget install UB-Mannheim.TesseractOCR`。二进制缺失时静默降级(标题走纯规则,不报错)。
pytesseract>=0.3.10
