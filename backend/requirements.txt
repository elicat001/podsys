fastapi>=0.115
uvicorn[standard]>=0.30
pillow>=10.4
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
pyjwt>=2.8
openai>=1.55
pytest>=8.0
# 本地抠图引擎(默认 POD_MATTING_PROVIDER=rembg 时必需;首次运行会下载 ~170MB U2Net 模型)
rembg>=2.0.59
onnxruntime>=1.18
# 本地 AI 超分(POD_UPSCALE_PROVIDER=onnx)用 onnxruntime 跑 Swin2SR x4 模型;
# 模型文件 backend/models/swin2sr_x4.onnx(~52MB,不入库)需另行放置,缺失时自动降级 Lanczos。
# 印花提取的本地去布料(洪水填充)用到;通常随 rembg 一起装上
numpy>=1.26
scipy>=1.11
# cv2:矢量化降级引擎(轮廓追踪)+ 快速超分 FSRCNN(dnn_superres,在 contrib 版里)。
# 用 contrib-headless(含 dnn_superres + 全部 base 功能);别同时装 opencv-python(会冲突坏 cv2)。
opencv-contrib-python-headless>=4.8
# 转矢量图主引擎:vtracer 工业级彩色描摹(平滑贝塞尔曲线 + 去碎块);缺失时降级到 cv2 轮廓追踪
vtracer>=0.6
