# docs/plans — 迭代计划 / 模块设计文档

了解当前项目请优先看这两份(最权威):

- 根 [`../../README.md`](../../README.md) —— 现状全景、系统分层、技术栈
- [`../../CLAUDE.md`](../../CLAUDE.md) —— 架构、红线、部署、运维细节(最权威)

## 📌 当前活跃模块文档(反映现状,不是归档)

- [`2026-06-23-video-pipeline.md`](2026-06-23-video-pipeline.md) —— **图生视频管线**(演进最密集的模块):
  双分镜 15s、智能方案向导、多语言旁白+字幕、场景母帧一致性、provider 双重重试、后期层(parked)、
  「呆板」痛点的设计取舍与 roadmap。**视频相关改动先读它。**

## 历史迭代计划(归档,不代表当前状态)

下面各文件是批次开发时的**实现计划**,记录"当时打算怎么做 / 分工 / 验收点",仅作历史参考。

### ⚠️ 与现状的差异(部分历史计划已过时)

- **可视化工作流编排 / 自定义工作流**(原 batch8)—— **已移除**(前后端 + `saved_workflows` 表都已删)。对应计划文档已删除。
- **DIY 编辑器** —— **已移除**。
- 历史计划里出现的 `frontend/*.html` 静态页 —— 已被 `frontend-vue/` 的 Vue 3 SPA 取代。
- 存储从"纯本地文件"演进为"本地盘 + 可选 MinIO/S3 对象存储"。

其余批次(异步/计费、工具矩阵、采集/店铺、深度/我的空间、标题 AI 视觉等)描述的功能基本仍在,可作历史参考。
